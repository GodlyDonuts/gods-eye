//! Optional offline-tooling bindings for Gods Eye (model export, evaluation
//! harnesses, dataset prep).
//!
//! These are a developer convenience only and never run on the real-time path.
//! `pyo3`/`maturin` bindings are added behind a feature in a later milestone;
//! this crate is an intentional placeholder so the default workspace build
//! requires no Python toolchain.

/// Placeholder marker; replaced by real bindings later.
pub const PLACEHOLDER: bool = true;
